# 演进路线：从课程 Demo 到工业级布局工具

> 本文档不属于本期任务书（`TASKBOOK.md`）范围。任务书刻意收窄了范围（自定义 CAD 规范 v1、占位方块、无后端），以保证 demo 可交付。
> 本文档回答的是下一个问题：**如果把它做成真正能给工厂规划人员使用的工业级工具，还差什么、打算怎么做。**

## 现状盘点：Demo 与工业级之间的差距

| 维度 | Demo 现状 | 工业级要求 |
|---|---|---|
| CAD 输入 | 自定义 CAD 规范 v1，只对自带样例 DXF 负责 | 兼容真实工厂的 DXF/DWG：图层混乱、单位/比例不一、块带属性、文字标注即设备名 |
| 设备表达 | 4 种彩色占位方块 + 中文标签 | 可参数化的真实设备模型库（glTF），尺寸/接口/安全区随型号走 |
| 布局能力 | 手动拖拽 + 吸附 | 安全间距校验、物流通道宽度校验、碰撞检测、动线可视化 |
| 方案产出 | 手工摆一个方案 | 生成多个候选布局并按动线长度/面积利用率评分排序 |
| 协作与交付 | 本地 JSON 文件 | 方案版本管理、导出报告（图纸 + 设备清单 + 指标） |

这个差距就是本项目停滞的真实原因：**工作量是 demo 的数倍，且每一块都要啃专业规范**（真实 DXF/DWG 格式、工厂设计安全规范、布局优化算法）。对学生个人开发者来说，纯手写不现实——所以演进方式定为 **AI Agent 结对开发**：我负责需求判断、规范核对与验收，AI 负责大代码量的实现与重构，用 `AGENTS.md` 约束协作边界。

## 阶段规划

### M4 · 真实 CAD 兼容层

- 从"只认规范 v1 样例"走向"能读真实工厂图纸"：自动识别图层语义（墙/柱/门/设备）、处理单位与比例换算、解析块属性与 TEXT 标注推断设备类型；
- 解析失败可降级：标出无法识别的图层交人工指定，而不是直接报错；
- AI 的角色：真实图纸样本的 case 分析、解析规则的大量分支实现、回归自证 fixture 的生成。

### M5 · 真实设备模型库

- 占位方块 → glTF 模型 + 参数化尺寸（长宽高、安全操作区、物料接口位置）；
- 模型缺失时回退占位方块，保证"数据契约不能持久化的能力不上 UI"的原则不变；
- AI 的角色：模型管线搭建、设备参数 schema 设计、批量模型格式转换脚本。

### M6 · 布局工程校验

- 安全间距校验（设备间、设备与墙/柱）、物流通道宽度校验、包围盒碰撞检测；
- 物流动线可视化：按工艺顺序生成物料流动路径并标出交叉冲突点；
- AI 的角色：工厂设计规范的条目化整理、几何算法实现、校验规则的可配置化。

### M7 · AI 布局助手

- 自然语言描述需求（"两条产线，共用一台 AGV，原料从东门进"）→ 自动生成多个候选布局；
- 按动线总长度、面积利用率、通道合规性给候选方案评分排序；
- 这是 SceneSpec 数据契约的自然延伸：布局本身就是一份 JSON，AI 生成 spec 即可渲染。

## 为什么这个路线适合 AI Agent 开发

1. **契约清晰**：SceneSpec 是唯一事实来源，AI 生成的任何布局/解析结果都可被程序自动验证，幻觉有兜底；
2. **边界可验收**：每个阶段都有"导入文件 → 输出可检查的结果"的闭环，适合 Agent 按任务书式指令执行；
3. **代码量大但模式重复**：解析分支、校验规则、模型管线正是 Agent 擅长的结构性工作，人来做判断题而非苦力题。

## 当前状态

M4–M7 均为**规划中**。开发工具计划以 AI 编程 Agent（如 MiMo Desktop）为主力，协作约定沿用本仓库 `AGENTS.md` 与 `DEVELOPMENT.md` 第 5 节的流程。
