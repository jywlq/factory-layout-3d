# 项目任务书 v2（修订版）

> 修订说明：v1 由 AI 生成，混淆了"老师要求"与"AI 自行发明"的边界，且把本期核心（CAD→3D）写成了第二期。
> 本版按老师原话重新对齐主线。文中 **〔老师要求〕** 为需求唯一来源；**〔项目假设〕** 为项目组自行决策，可调整，偏离时记录在案。

## 1. 需求来源（老师原话，唯一需求依据）

1. 本期**仅建设 CAD 平面图至三维场景的自动生成能力**：将符合规范的 CAD 平面图转换为可浏览、可编辑、可保存的三维工厂场景。
2. **不建设**任何生产仿真、节拍计算、动态物流、仿真动画、设备实时数据接入或数字孪生能力。
3. 目标：**快速给客户展示未来厂房和设备的面貌**；尽快出一个 demo。

## 2. 定位与交付标准

交付物是一个**能在客户面前流畅演示**的 Web demo。验收不看代码量，看演示效果：
导入一份 DXF → 自动生成三维厂房 → 现场微调 → 保存/截图带走。任何功能若不能服务这条演示链路，一律不做。

## 3. 范围

### 做

| 阶段 | 内容 | 状态 |
|---|---|---|
| 地基（原 M1） | 3D 浏览 / 设备添加、拖拽、旋转 / 网格吸附 / 顶视图 / 场景保存加载 / 截图导出 | ✅ 已完成 |
| **CAD→3D（原 M2，本期主线）** | 上传符合规范的 DXF → 自动生成地面/墙/设备占位，生成后仍可编辑保存；仓库内置样例 | 待实施 |
| 打磨交付（原 M3） | 全中文 UI 收尾、配色与名称标签、README（含 CAD 规范 v1 与已知限制）、演示脚本演练 | 待实施 |

### 禁止做、禁止引入相关依赖〔老师要求〕

DWG/STEP/IGES 解析；真实 glTF 模型加载；WebGPU 渲染器；后端/数据库；仿真/物流/AGV 调度/实时数据；登录/权限/国际化；React/Vue/CSS 框架；测试框架。

### 依赖白名单〔项目假设〕

Vite + TypeScript（vanilla 模板）+ three + dxf-parser（仅 CAD→3D 用）。除以上不新增依赖。UI 用原生 HTML/CSS，界面文字全部中文。

## 4. 演示脚本（验收即演练）

1. 打开页面即见内置示例小工厂，可旋转/缩放浏览；
2. 点"导入 CAD(DXF)"，选择内置 `fixtures/sample.dxf`（演示时不依赖客户现场掏文件）；
3. 状态栏显示解析结果（墙数/设备数），三维场景自动生成；
4. 切顶视图——朝向与客户 CAD 平面图一致（对 CAD 出身的客户是强信任点）；
5. 现场微调：拖动/旋转一台设备、添加一台新设备，展示"可编辑"；
6. 保存场景 JSON、导出当前视角 PNG 截图（客户可带走）。

## 5. 技术架构（硬性条款，不得绕过）

- **场景数据契约 SceneSpec**（JSON，单位米，Y-up）：

  ```json
  {
    "floor": { "w": 0, "d": 0 },
    "walls": [{ "x": 0, "z": 0, "len": 0, "h": 0, "rotY": 0 }],
    "devices": [{ "id": "", "type": "", "x": 0, "z": 0, "rotY": 0 }]
  }
  ```

- **纯函数 `buildScene(scene, spec)`**：清空旧对象后按 spec 整体重建；所有渲染只认这份 JSON。
- **编辑闭环**：一切交互（添加/拖拽/旋转/删除/导入）只修改 spec，再重建场景。
- **界面能力 ≤ 数据能力**：数据契约不能持久化的操作，一律不在 UI 中提供（本项目两条已确认的约束：高度锁定贴地、旋转仅限绕竖直轴）。

## 6. CAD 规范 v1（规范由我们定义，解析器只对本规范负责）

- ASCII DXF，单位米。
- **FLOOR 层**：闭合 LWPOLYLINE = 地面轮廓，取其包围盒作为 floor 的 w/d（居中对齐）。
- **WALL 层**：LINE / LWPOLYLINE 的每个直线段 = 一段墙中心线（len=段长，rotY=段方向；弧段允许折线近似）。
- **设备**：INSERT 块，块名 ∈ 设备目录（不区分大小写）；插入点 = 设备中心；块旋转角 = 设备朝向。
- 缺 FLOOR 时回退：WALL 包围盒外扩 1 m。
- 坐标映射：DXF(x,y) → three(x,z)，保证顶视图的右/上 = CAD 的右/上，角度方向一致。
- 必须自带一个已知坐标的 fixture 做自证（解析结果与手算值一致，dev 模式 `console.assert`）。

## 7. 默认假设（可调，不是需求）〔项目假设〕

以下由 v1 任务书发明，实现时可按效果调整，但同一处只允许有一个事实来源：

- 设备目录（类型/中文名/尺寸/颜色）：以 `src/builder.ts` 的 `DEVICE_PRESETS` 为准（CNC 机床-蓝 / 货架-绿 / 托盘区-橙 / AGV-黄）；
- 吸附步长：平移 0.5 m、旋转 15°，带开关；
- 默认示例工厂：`src/defaultSpec.ts`（30m×20m、外墙一圈、若干设备）；
- 模块文件命名：以现有代码为准（spec/builder/controls/io/ui/main），不强求 v1 中的 catalog.ts/editor.ts 等命名。

## 8. 假设与风险（对客演示时主动声明）

1. CAD 规范 v1 由我们定义，demo 只对自带样例负责，**不承诺解析客户任意 DXF/DWG**（老师所说"符合规范"的规范解释权在我们）；
2. **设备只支持平面布局**：落地 + 绕竖直轴转向；高度与倾斜不在本期范围（已确认维持）；
3. 设备为彩色占位方块 + 中文标签，非真实设备模型；
4. 无后端，场景以 JSON 文件形式在浏览器本地保存/加载。

## 9. 工程要求

- `src/` 分模块，中文注释；`npm run dev` 直接可跑；`npm run build` 通过；控制台无报错。
- README 含：项目说明、运行步骤、功能清单、SceneSpec 格式、CAD 规范 v1、已知限制。
- 工作原则：有歧义选最简单实现；不做超出需求的重构；不加依赖；可读性 > 技巧性；拿不准的产品决策写进"我的假设"，不默默自作主张。

## 10. 验收清单（逐条跑并如实报告）

- [ ] dev 打开即见示例小工厂，可旋转缩放
- [ ] 四种设备可添加；拖拽 0.5m 吸附、旋转 15° 吸附；吸附可开关
- [ ] Esc 取消选中；Delete 删除
- [ ] 保存 JSON → 刷新清空 → 加载恢复一致（含旋转角度）
- [ ] 截图下载 PNG
- [ ] 顶视图切换且朝向与 CAD 一致
- [ ] 导入 sample.dxf 生成地面/墙/设备，位置与 fixture 注释一致
- [ ] 导入后可编辑（移动/旋转/添加/删除）并可再次保存
- [ ] build 通过、无 console 报错
